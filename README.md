# mdgarden
MDGarden is a lightweight, portable, markdown-based wiki/document viewer that runs entirely in the browser.
It works as a simple markdown viewer on a public static server, and as a personal knowledge base on localhost.
Because it is built from static files only, setup is easy, portability is high, and the server-side attack surface stays very small.
